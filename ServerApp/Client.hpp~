#ifndef __CLIENT
#define __CLIENT

#include <thread>
#include <unistd.h>
#include <iostream>
#include <strings.h>
#include <stdlib.h>

class Client
{
public :
  Client(int, int);
  ~Client();
  void readData(int socketfd);
private :
  int socketfd;
  int ID;
  bool running;
  std::thread * backgroundReadingThread;
};

#endif


